/* groovylint-disable CompileStatic, DuplicateStringLiteral */
pipeline {
    agent any
    stages {
        stage('Ansible') {
            steps {
                echo 'Ansible'
                sh '''cd ./ansible
                ansible-playbook playbook.yml'''
            }
        }
    }
}
