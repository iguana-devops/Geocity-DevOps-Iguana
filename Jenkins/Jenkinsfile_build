/* groovylint-disable DuplicateStringLiteral, LineLength, NestedBlockDepth */
pipeline {
    agent any
    tools {
        jfrog 'jfrog-cli'
        maven 'maven'
    }
    environment {
        // CI = 'true'
        // ARTIFACTORY_ACCESS_TOKEN = credentials('artifactory-token')
        WORKING_DIR = '/var/lib/jenkins/workspace/build_and_push_artifact/java-app'
    }
    stages {
        stage('Build artifac') {
            steps {
                echo 'Maven build'
                sh "pwd ${WORKSPACE}"
                // dir("${WORKING_DIR }") {
                //         sh 'mvn clean install -U'}
            }
        }
            // stage('Push') {
            //     steps {
            //         echo 'Push to jfrog repository'
            //         dir("${WORKING_DIR }/target") {
            //             jf "rt u  citizen-${JOB_NAME}-${BUILD_NUMBER}.war maven-repository/"}
            //     }
            }
    }
    post {
        always {
            cleanWs()
        }
    }
}
