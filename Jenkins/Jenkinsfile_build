/* groovylint-disable DuplicateStringLiteral, LineLength, NestedBlockDepth */
pipeline {
    agent any
    tools {
        jfrog 'jfrog-cli'
        maven 'maven'
    }
    environment {
        // CI = 'true'
        // ARTIFACTORY_ACCESS_TOKEN = credentials('artifactory-token')
        WORKING_DIR = '/var/lib/jenkins/workspace/build_and_push_artifact/java-app'
    }
    stages {
        stage('Build artifac') {
            steps {
                echo 'Maven build'
                dir("${WORKING_DIR }") {
                        sh 'mvn clean install -U'}
            }
        }
            stage('Push') {
                steps {
                    echo 'Push to jfrog repository'
                    dir("${WORKING_DIR }") {
                        jf 'c show'
                        jf 'rt ping'
                        sh 'touch test-file'
                        jf 'rt u test-file maven-repository/'
                        jf 'rt u  target/citizen.war maven-repository/'
                        jf 'rt bp'
                        jf 'rt dl maven-repository/test-file'}
                }
            }
    }
    post {
        always {
            cleanWs()
        }
    }
}
